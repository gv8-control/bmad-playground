# Trace Remediation Plan — 2026-07-07

From the 2026-07-07 traceability gate run (PASS, SHA `c06ad51`). Each issue maps to a BMAD skill and prompt. Run in the listed order.

## Execution Order

1. **REDACTED** — DO nothing
2. **Investigate 3.3-AC7 flakiness** — Skill: `bmad-investigate` — Prompt: `investigate the flaky scroll-pause assertion in playwright/e2e/conversation/streaming-chat.spec.ts for 3.3-AC7 — determine whether the flakiness is a timing issue, selector issue, or state-transition race`
3. **Add 1.6-AC3 integration test** — Skill: `bmad-testarch-automate` — Prompt: `expand test coverage for 1.6-AC3 — add a full-cycle re-auth integration test that seeds a failed credential, calls reauthorizeGitHub, and asserts getCredentialHealth returns 'healthy' and repoUrl is unchanged`
4. **Fix 1.8-AC4 weak assertions** — Skill: `bmad-quick-dev` — Prompt: `fix the weak-assertion tests in AppShell.test.tsx:68-88 — add expect(screen.getByTestId('sheet-content')).toBeVisible() after the click in the first test and expect(screen.queryByTestId('sheet-content')).not.toBeInTheDocument() after Escape in the second`
5. **Add 3.6-AC7 help-text test** — Skill: `bmad-testarch-automate` — Prompt: `expand test coverage for 3.6-AC7 — add a test asserting the working-tree indicator's explanatory help text is reachable when the indicator is in the dirty state`
6. **Fix 3.3-AC7 flakiness** — Skill: `bmad-quick-dev` — Prompt: `fix the flaky scroll-pause E2E test in streaming-chat.spec.ts for 3.3-AC7 based on the investigation findings — replace any hard waits with deterministic waits per the test-quality DoD`
7. **Add P2/P3 coverage** — Skill: `bmad-testarch-automate` — Prompt: `expand test coverage for low-priority uncovered requirements: 1.1-AC1 (Nx workspace build — confirm intentional exclusion first), 1.1-AC2 (Tailwind theme tokens match DESIGN.md), and Epic 3 P2/P3 polish items`
8. **Suite-wide test review** — Skill: `bmad-testarch-test-review` — Prompt: `lets review tests across the full suite (87 files, ~1042 cases) — focus on the expanded Epic 3 conversation tests and the newly added hydration, contract, and E2E isolation tests from the 16-commit delta`
9. **Add NFR-R4 concurrent SSE integration test** — Skill: `bmad-testarch-automate` — Prompt: `Write an integration test for NFR-R4 (10 concurrent SSE connections without starvation) in apps/agent-be/test/integration/. Context: NFR-R4 requires the SSE transport to support 10 concurrent agent SSE connections per browser session without connection starvation (matching FR11's 10-conversation limit). The browser HTTP/1.1 limit of 6 connections is bypassed by an HTTP/2 reverse proxy at the deployment layer — that part is a launch checklist verification, not testable here. What IS testable is the server-side behavior: NestJS must handle 10 concurrent SSE streams without resource contention, event loss, or starvation across conversations. What to test: open 10 concurrent SSE connections to 10 distinct conversations via the GET /api/conversations/:id/events?token=<boundary-jwt> endpoint; emit events on all 10 conversations via SessionEventsService.emit(); assert all 10 connections receive their events (no starvation); assert events from one conversation don't leak into another (isolation). Follow existing patterns: use buildTestModule from test/helpers/test-module-builder (same as sandbox-lifecycle.integration.spec.ts); inject SandboxServiceFake (no real Daytona); mint boundary JWTs using the jose pattern from streaming.controller.spec.ts (SignJWT, HS256, issuer bmad-easy:boundary, audience bmad-easy:agent-be, 8h expiry, keyed by AUTH_SECRET); the SSE endpoint is StreamingController.stream() at GET /api/conversations/:id/events — it validates the boundary JWT from the ?token= query param (EventSource cannot set headers), does findFirst({ where: { id, userId } }) for tenant isolation, then sets SSE headers and subscribes to sessionEvents.getEventStream(conversationId); use Node's http module or fetch with streaming to consume SSE responses (no browser EventSource in Node test environment). File: apps/agent-be/test/integration/sse-concurrent-connections.integration.spec.ts. Test config: runs via jest-integration.config.ts (already configured for **/*.integration.spec.ts, maxWorkers: 1, 30s timeout). Tag the test [P0] — it validates an NFR that is a release gate requirement. Verify: run yarn nx run agent-be:test-integration and confirm the test passes alongside the existing sandbox-lifecycle.integration.spec.ts.`

## Notes

- Steps 3, 4, and 5 are independent and can run in parallel across sessions.
- Step 6 depends on step 2's investigation findings.
- Step 8 should run last so it reviews all tests including those added in steps 3-7 and 9.
- Step 9 is independent and can run in parallel with steps 3-5.
- All issues are non-blocking (gate decision: PASS). Priorities: 3, 4, and 9 are MEDIUM, the rest are LOW.
