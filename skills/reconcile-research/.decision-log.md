---
phase: complete
classification: simple-workflow
last_touched: 2026-06-14
---

# Decision Log — reconcile-research

## Session 2026-06-14

**Skill name:** `reconcile-research` — reflects the reconciliation action over the document set.

**Classification:** Simple Workflow — four stages fit inline in SKILL.md (~65 lines, well under the 130-line ceiling). No carve-outs needed.

**Scan scope:** All `*.md` files inside any `research/` subdirectory under `{output_folder}`, found via `rglob("research")`. Covers both `_bmad-output/research/` and `_bmad-output/planning-artifacts/research/` automatically.

**Conflict resolution priority:**
- (a) Newer `updated:` frontmatter date
- (b) Greater depth/specificity on the specific topic
- (c) Simpler of the two competing claims

Git modification timestamp was considered as (b) and rejected by the user — `updated:` frontmatter is the only date signal.

**No inline reconciliation comments:** `<!-- reconciled ... -->` markers considered; rejected. Response changelog and `git diff` are sufficient for review.

**No reconciliation report file:** A separate `reconciliation-{date}.md` output file considered; rejected. The response summary is the changelog.

**Fully non-interactive:** Confirmed by user. No pause after categorization for grouping review.

**Customization:** None — ships fixed. Scan path derives from core config `{output_folder}`.

**Script for discovery:** `scripts/discover_research_docs.py` handles deterministic file listing and frontmatter date extraction. Judgment (categorize, reconcile) stays in the prompt layers.

**Sub-agent per domain group (parallel):** One sub-agent per group, writes in-place using its Edit tool, returns compact JSON override summary to parent for changelog compilation.
